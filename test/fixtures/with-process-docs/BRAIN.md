# BRAIN

This is a project's persistent context file (agent uses it to remember architecture decisions, conventions, etc.). Discover should detect it as an agent-context file via the capitalized-root-md heuristic.
