<!-- VIBE-KIT:PROMPT-BLOCK-START
The /vibe-retrofit skill reads this block to know what questions to ask the user
when drafting this file. Block stripped before final write.

Questions to ask (with discovery-context priming):

1. TEST COMMAND — "What's the actual test command? Discovery guessed `{{test_command}}`
   — confirm or correct."
   Prime with: .commands.test from discovery.

2. TEST LAYOUT — "Where do tests live and what's the naming convention? (`*.test.ts`
   next to source? `tests/` mirroring `src/`? `_spec.rb`?)"
   Prime with: a quick `find` of common test file patterns the script ran.

3. MOCK BOUNDARY — "What's mocked in tests vs. hit for real? (Network calls? DB?
   Filesystem? Third-party APIs against test accounts?)"

4. DOMINANT PATTERN — "Describe the most-used test pattern in this repo in 3-5
   lines so future-you doesn't have to re-derive it. (E.g., 'set up fixtures in
   beforeAll, hit real PGLite, snapshot the response, teardown via TRUNCATE.')"

5. HARD-TO-TEST AREAS — "Be honest: which components are awkward to test?
   (UI state machines, background jobs, async event loops, third-party-API-
   dependent paths.) These are usually where bugs ship — name them so future-you
   gives them extra scrutiny."

6. COVERAGE PHILOSOPHY — "Are you targeting 100% line coverage? Just critical
   paths? Are integration tests valued more than unit tests? State the taste so
   future-you doesn't drift."

Output: fill the {{SLOT_*}} placeholders.
VIBE-KIT:PROMPT-BLOCK-END -->

# Testing — {{project_name}}

Curated by `/vibe-retrofit` on {{retrofitted_at}}.

## Test command

```
{{SLOT_TEST_COMMAND}}
```

## Test layout

{{SLOT_TEST_LAYOUT}}

## Mock boundary

{{SLOT_MOCK_BOUNDARY}}

## Dominant test pattern

{{SLOT_DOMINANT_PATTERN}}

## Hard-to-test areas (give extra scrutiny)

{{SLOT_HARD_TO_TEST}}

## Coverage philosophy

{{SLOT_COVERAGE_PHILOSOPHY}}

## Updating this file

When the testing surface changes (new test framework, new mock strategy, new
hard-to-test area), update the relevant section. Refresh from scratch by
re-running `/vibe-retrofit`.
