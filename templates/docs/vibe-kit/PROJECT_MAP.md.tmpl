# Project Map — {{project_name}}

This file is a TODO scaffold. Fill it in YOURSELF — do not have Claude draft it. The point is your synthesis of what's load-bearing.

{{existing_docs_pointer_block}}

## Entry points

What are the 5 most important entry points into this codebase? (Routes, CLI commands, exported functions, background jobs.)

- [ ] (entry point 1 — file:line)
- [ ] (entry point 2 — file:line)
- [ ] (entry point 3 — file:line)
- [ ] (entry point 4 — file:line)
- [ ] (entry point 5 — file:line)

## Module structure

What lives where? Keep it to one line per top-level directory. Skip anything obvious from the name.

- `src/` —
- `lib/` —
- ...

## Data model

What are the 5 most important data entities? Where are they defined?

- [ ]
- [ ]
- [ ]
- [ ]
- [ ]

## Where new code usually goes

When you add a feature, where does it usually land? What's the typical file dance? (e.g., "controller → service → model → migration → test.")

## Who reads this file

Future Claude. Future you. New collaborators. Update it when one of those people would have been confused.
